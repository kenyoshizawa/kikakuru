$(function() {
  $('.started-clockpicker').clockpicker({
    // default: `13:00`,
    autoclose: true,
  });

  $('.finished-clockpicker').clockpicker({
    // default: `19:00`,
    autoclose: true,
  });
});
